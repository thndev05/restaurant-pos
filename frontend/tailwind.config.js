/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS v4 uses @theme in CSS files instead of this config
  // Keep this file for compatibility but main configuration is in src/index.css
}
